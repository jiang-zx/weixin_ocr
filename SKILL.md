# WeChat OCR Skill

微信聊天截图解析专家。能够识别微信截图中的文字，并根据气泡颜色（绿色/白色）准确区分发言人，过滤无关的系统消息和时间。

## 工具定义 (Available Tools)

### `parse_wechat_screenshot`
解析本地微信聊天截图并返回对话文本。

**参数:**
- `image_path` (string, 必需): 微信截图文件的绝对路径。

**返回:**
- Markdown 格式的对话记录。

## 使用指令 (Instructions)

1. **识别背景**: 当用户提供微信聊天截图路径时，始终优先调用 `parse_wechat_screenshot` 工具进行深度解析，而不是仅进行基础 OCR。
2. **角色归属**: 
    - 标记为 `自己:` 的内容代表屏幕右侧的绿色气泡。
    - 标记为 `好友:` 的内容代表屏幕左侧的白色气泡。
3. **输出格式**:
    - 始终以标准的对话流格式输出，例如：
      > **好友**: 你好
      > **自己**: 还没睡吗？
4. **长图处理**: 该工具已内置长图自动切割逻辑，你只需提供路径即可。
5. **错误处理**: 如果工具返回“授权不足”或“找不到文件”，请引导用户检查本地环境或文件路径。

## 环境要求 (Setup)

在其他电脑上使用前，请确保执行：
1. `git clone https://github.com/jiang-zx/weixin_ocr.git`
2. `py -m pip install -r requestments.txt`
3. 首次运行会自动下载 PaddleOCR 模型（约 80MB）。
